name: IPTV
description: IPTV Viewer

publish_to: "none"

version: 1.0.0+1
environment:
  sdk:
  flutter:

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  animations:
  dio:
  dio_cookie_manager:
  dio_http2_adapter:
  dio_flutter_transformer:
  freezed_annotation:
  logger:
  meta:
  intl:
  characters:
  flutter_staggered_animations:
  json_annotation:
  archive:
  video_player:
  # video_player_web:
  video_player_web_hls:
    git:
      url: https://github.com/Mravuri96/video_player_web_hls.git
      ref: bfb8651a44029011ea6ba39ea92cb1299a41f0ea
  get:
  flutter_hooks:
  hooks_riverpod:
  dartx:
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  build_runner:
  build_web_compilers:
  freezed:
  quiver:
  hive_generator:
  retrofit_generator:
  json_serializable:
  pedantic:
  effective_dart:

flutter:
  uses-material-design: true
  assets:
    - assets/streams.zip
    - assets/channels.json
    
scripts:
  build: flutter pub run build_runner build --release --delete-conflicting-outputs
  watch: flutter pub run build_runner watch --watch --delete-conflicting-outputs